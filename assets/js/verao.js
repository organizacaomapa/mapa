
  // Initialize carousels
  var carouselSlideShowI = new bootstrap.Carousel(document.getElementById('carouselSlideshowI'), {
    interval: 5000, // Change the interval as needed
    wrap: true
  });

  var carouselSlideShowII = new bootstrap.Carousel(document.getElementById('carouselSlideshowII'), {
    interval: 5000, // Change the interval as needed
    wrap: true
  });